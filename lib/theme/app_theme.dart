import 'package:flutter/material.dart';

class AppTheme {
  static const Color background = Color(0xFF1A1410); // very dark brown
  static const Color card = Color(0xFF2A2018); // slightly lighter brown
  static const Color accent = Color(0xFFD4820A); // warm amber
  static const Color textPrimary = Color(0xFFF5E6C8); // warm white
  static const Color textMuted = Color(0xFF8B7355); // muted gold
}
